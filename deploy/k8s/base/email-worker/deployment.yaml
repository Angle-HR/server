apiVersion: apps/v1
kind: Deployment
metadata:
  name: email-worker
  labels:
    app.kubernetes.io/name: email-worker
    app.kubernetes.io/component: email-worker
    app.kubernetes.io/part-of: anglehr
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: email-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: email-worker
        app.kubernetes.io/component: email-worker
        app.kubernetes.io/part-of: anglehr
    spec:
      containers:
        - name: email-worker
          image: ghcr.io/angle-hr/server-email-worker:latest
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: anglehr-config
            - secretRef:
                name: anglehr-secrets
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - kill -0 1
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
