apiVersion: v1
kind: Service
metadata:
  name: fluvio-ui
  labels:
    app.kubernetes.io/name: fluvio-ui
    app.kubernetes.io/component: ui
    app.kubernetes.io/part-of: anglehr
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: fluvio-ui
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
