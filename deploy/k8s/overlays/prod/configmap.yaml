apiVersion: v1
kind: ConfigMap
metadata:
  name: anglehr-config
  labels:
    app.kubernetes.io/part-of: anglehr
data:
  SERVER_PORT: "8080"
  APP_ENV: production
  PUBLIC_API_URL: https://api.example.com
  CORS_ALLOWED_ORIGINS: https://app.example.com
  FLUVIO_UI_ORIGIN: https://fluvio.example.com
  JWT_ACCESS_TTL: "3600"
  JWT_REFRESH_TTL: "604800"
  AUTH_DEFAULT_REGION: uk
